VITE_CLERK_PUBLISHABLE_KEY=YOUR_PUBLISHABLE_KEY

# Used by Convex/Clerk integration (set this via `npx convex env set ...` for deployments)
CLERK_FRONTEND_API_URL=YOUR_CLERK_FRONTEND_API_URL

VITE_CONVEX_URL=YOUR_CONVEX_URL

# Deployment used by `npx convex dev`
CONVEX_DEPLOYMENT=YOUR_CONVEX_DEPLOYMENT

VITE_CONVEX_SITE_URL=YOUR_CONVEX_SITE_URL

# Optional (used only when Diagnostics consent is enabled)
VITE_SENTRY_DSN=YOUR_DSN

# Used by Convex httpAction CORS for export downloads (set via `npx convex env set CLIENT_ORIGIN ...`)
CLIENT_ORIGIN=YOUR_APP_ORIGIN
